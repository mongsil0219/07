#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	{
		int flag = 1;
		int y;
		while(flag != 0)
		{
			int y;
			y = 3;
			flag = 0;
		}
		y = 4;
		// y 정의X 함수내에서만 실행되고 끝났음
	}
	return 0;
}
